---
tags:
  - project-prime
  - gap
type: gap
status: open
identified-from:
  - "[[<source-note-1>]]"
  - "[[<source-note-2>]]"
filled-by: []
---

# 🟡 Gap: __<ProblemName>__

> Gap note. `status: open` = identified but unresolved. `status: partially-filled` = some partial approaches in the vault but no full resolution. `status: filled` = fully resolved (consider archiving or converting to a Design/Arch note). Surface this gap when a question touches __<the unresolved domain>__.

## What the gap is

__Clear description of what is unsolved or missing. State the question, not the speculation.__

__If applicable: distinguish the resolved-so-far state from what is still open.__

- **What's settled:** __decision banked + when + by whom__
- **What's still open:** __the unresolved question + the trigger that would force a decision__

## Why it matters

__What would be possible if this were resolved? What's the cost of leaving it open?__

__Concrete consequences: skill paths that can't be designed yet, scenarios that block, etc.__

## Partial approaches in the vault

- [[<note-1>]] — __how it gets close but doesn't fully address__
- [[<note-2>]] — __how it gets close but doesn't fully address__

## Triggers that would resolve / change this

- __Trigger 1__ (e.g., "advisor confirms a staging server is available with `module load amber`"). → __status would become__.
- __Trigger 2__ (e.g., "demo successfully runs locally for 6 months without HPC need"). → __status would become__.

## Cross-links

- [[Project Prime]] — primary roadmap
- [[Dev_Log]] entries __YYYY-MM-DD__, __YYYY-MM-DD__ — sessions where this gap surfaced

## Maintenance

When this gap shifts (status change, partial fill, new trigger met), update the frontmatter `status:` + `filled-by:` AND write a Dev_Log entry citing the change. Do not silently mutate.
